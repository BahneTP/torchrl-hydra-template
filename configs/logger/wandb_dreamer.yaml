# @package _global_
logger:
  - _target_: src.callbacks.logger.WandBLogger
    project: torchrl-hydra-template
    entity: latentlab
    name: ${hydra:runtime.choices.algorithm}_${hydra:runtime.choices.environment}_${model_size}_${now:%Y-%m-%d_%H-%M-%S}
    tags: []
    mode: online
    save_dir: ${paths.log_dir}/wandb/
