# Atari-100k DER train environment. Rewards stay raw, frame skip stops on life
# loss, and the soft reset continues without an additional NOOP action.
task: Jamesbond
name: ALE/${environment.task}-v5
action_repeat: 4
gym_backend: gymnasium
gym_kwargs:
  frame_skip: 1
  repeat_action_probability: 0.0
  disable_env_checker: true
  from_pixels: true
  pixels_only: false
  categorical_action_encoding: false

gymnasium_wrappers:
  - _target_: src.environments.atari_wrappers.MaxAndSkipEnv
    skip: 4
    stop_on_life_loss: true
  - _target_: src.environments.atari_wrappers.NoopResetEnv
    noop_max: 30
  - _target_: src.environments.atari_wrappers.EpisodicLifeEnv
    advance_on_life_loss: false

transforms:
  - _target_: torchrl.envs.ToTensorImage
  - _target_: torchrl.envs.GrayScale
  - _target_: torchrl.envs.Resize
    w: 84
    h: 84
  - _target_: torchrl.envs.CatFrames
    N: 4
    dim: -3
  - _target_: torchrl.envs.RewardSum
  - _target_: torchrl.envs.StepCounter
    max_steps: 27000
